# Data directory

This directory is intentionally empty.

Do not upload private mammography images, DBT-derived images, pathology reports, patient identifiers, or institution-specific metadata to the public repository.

Use `examples/` for schema templates and synthetic examples only.

